---
scope: Smart Dispatch IA v2.1 whole-system architecture
purpose: Build substrate plus academic course-delivery explanation
altitude: feature
updated: 2026-07-27T22:16
---

- (constraint) The spine binds PRD FR-1 through FR-21 and NFR-1 through NFR-7; product defaults marked ASSUMPTION remain configurable and traceable.
- (constraint) Brownfield reality is one Python process serving a vanilla JavaScript SPA; preserve Python and browser assets while replacing the monolithic handler incrementally.
- (decision) AD-1 binds all units to a hexagonal modular monolith with a deterministic pipeline; this prevents API, database, and UI code from owning business decisions.
- (decision) AD-2 makes the orchestrator the sole owner of Dispatch Run state transitions through an explicit transition table; this prevents agent stages or HTTP handlers from skipping states.
- (decision) AD-3 requires feasibility to complete before scoring and gives the policy engine sole ownership of hard-constraint eligibility; this prevents memory or ranking from restoring an ineligible technician.
- (decision) AD-4 makes scoring and confidence pure deterministic domain functions over immutable snapshots and versioned configuration; this prevents hidden I/O and non-reproducible results.
- (decision) AD-5 gives repositories sole ownership of SQLite mutation within one unit-of-work transaction per command; this prevents partial run, decision, outcome, and learning writes.
- (decision) AD-6 separates immutable episodic evidence from derived semantic patterns and makes the learning service the only semantic-pattern writer; this prevents single observations from becoming active rules.
- (decision) AD-7 uses Pydantic models as the canonical stage/API contract and validates both sides of every stage boundary; this prevents schema drift among independently built stages.
- (decision) AD-8 exposes versioned /api/v1 commands and queries with a uniform envelope and idempotency key on mutating run/decision/outcome commands; this prevents duplicate mutations during retries.
- (decision) AD-9 keeps the existing vanilla JavaScript frontend as a replaceable adapter consuming only the API; this prevents UI state from becoming authoritative domain state.
- (decision) AD-10 persists UTC timestamps and captures one run clock/config/input snapshot at run creation; this prevents time-dependent rules from changing mid-run.
- (decision) AD-11 stores structured decision evidence and never private chain-of-thought; this prevents explanation requirements from coupling to model internals.
- (decision) AD-12 runs Capture and Analyze behind ports with deterministic local adapters as the MVP default and optional LLM adapters later; this prevents an external model outage from blocking the classroom demo.
- (decision) AD-13 defines a single-process local deployment with SQLite WAL, one writer transaction, startup migrations, and local file backup/export; this prevents unsupported multi-process writes and undeclared infrastructure.
- (decision) AD-14 makes scenario fixtures and run snapshots the source for KPI and memory-on/off comparison; this prevents comparisons from using different inputs.
- (decision) AD-15 enforces test layers: pure domain unit tests, repository integration tests, API contract tests, and browser smoke tests; this prevents passing demos with untested invariants.
- (version) Verified stable stack on 2026-07-27: Python 3.14.6, FastAPI 0.138.2, Pydantic 2.13.4, SQLAlchemy 2.0.51, Alembic 1.18.5, pytest 9.1.1, coverage.py 7.13.5.
- (decision) Use SQLAlchemy 2.0 Core rather than ORM mappings for persistence; explicit tables and row mappings keep the educational data flow visible and prevent implicit session state.
- (assumption) SQLite database file lives under data/smart_dispatch.db and local development is single-user/single-process; revisit before any external pilot.
- (assumption) The API and academic report are English contracts while the current UI may remain Spanish; user-facing localization is deferred.
- (question) Confirm retention and deletion policy before any external pilot; MVP retains local run evidence until explicit reset/export.
- (question) Confirm production authentication and HTTPS topology before any non-local deployment; both are outside MVP.
- (decision) AD-16 incorporates PRD behavioral defaults and evidence/KPI/accessibility contracts as v1 so parallel stories cannot choose incompatible formulas or fields.
- (decision) AD-17 separates atomic decision and later outcome commands with WAIT_FOR_OUTCOME, preventing a transaction across field service.
- (decision) AD-18 fixes stage semantics: PLAN owns eligibility then scoring; EVALUATE may validate/explain but not change rank.
- (decision) AD-19 keeps legacy /api routes as translation-only adapters until /api/v1 journey and error smoke tests pass.
- (decision) AD-20 commits each stage through optimistic run revision checks; stale transitions return 409 and cannot duplicate evidence.
- (decision) AD-21 preserves legacy external IDs and timezone provenance; imported JSON learning is inactive except in named synthetic fixtures.
- (decision) AD-22 binds local-only 127.0.0.1 serving, vendored assets, safe DOM rendering, backup-before-migration/reset, and fail-closed startup.
- (decision) AD-23 classifies legacy rules: driving limit and EPP are hard, priority-5 overtime is superseded, 50 km is soft, ineligible overrides are rejected.
- (version) Changed runtime seed from Python 3.14.6 to Python 3.12.10 and added uv 0.11.16 because the brownfield evaluator environment lacks 3.14 and Python 3.12.10 has full installers; uv.lock provisions the exact graph.
- (event) Input reconciliation completed and clear PRD/brownfield gaps applied.
- (decision) AD-24 makes the v1 score, component, penalty, and confidence normalization formulas normative, closing ranking divergence.
- (decision) AD-25 binds crash resume and exactly-once learning through uniqueness keys, a learning ledger, and retryable LEARN_FAILED.
- (decision) AD-26 binds Python provisioning, uv frozen lock, Uvicorn single-worker startup, SQLite migration gate, and Playwright browser target.
- (decision) AD-27 gives immutable snapshots, generated OpenAPI, KPI events, replay lineage, Memory snapshot isolation, and reset scope explicit owners.
- (version) Verified and added Uvicorn 0.46.0 and Playwright for Python 1.60.0; SQLite capability floor is 3.35.0 and runtime version is recorded.
- (event) Reviewer critical and high findings applied.
- (decision) AD-23 clarified that the 50 km rule never changes eligibility and always applies the AD-24 penalty.
- (event) Academic architecture passed structural review: conceptual explanation flows from thesis to components, execution, data, operations, testing, migration, alternatives, and traceability; diagrams and tables are preserved as comprehension aids.
- (event) Academic architecture passed prose review with no clarity-blocking issues identified.
- (event) Reviewer gate final verdicts: rubric pass with minor fixes applied, technology pass, adversarial seam pass ready for epic decomposition.
- (event) spine finalized
